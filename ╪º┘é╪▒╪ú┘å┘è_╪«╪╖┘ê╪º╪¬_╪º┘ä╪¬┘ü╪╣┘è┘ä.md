# تفعيل حقيقي لإضافة المستخدمين والتحكم في الصلاحيات — ROLLEX

## أولاً: ليه كان "مش شغال بجد"؟

بعد فحص كود المشروع الفعلي، لقيت 3 مشاكل جوهرية في `users.html`:

1. **إضافة مستخدم جديد كانت بتطرد الأدمن من حسابه.**
   الكود كان يستخدم `supabaseClient.auth.signUp()` من المتصفح مباشرة.
   في Supabase، `signUp` من طرف العميل بيسجّل دخول المتصفح تلقائياً
   بحساب المستخدم الجديد ويستبدل جلسة الأدمن الحالية. يعني عملياً
   الأدمن كان "يخرج من حسابه" ويدخل بحساب الموظف الجديد كل مرة يضيف
   حد جديد.

2. **الحذف النهائي كان "وهمي".**
   `hardDeleteUser` كان يمسح فقط صف `profiles`، ولا يمسح حساب
   الدخول الحقيقي (`auth.users`). يعني المستخدم "المحذوف نهائياً"
   يقدر يرجع يسجل دخول بنفس الإيميل وكلمة المرور القديمة.

3. **تعديل كلمة مرور مستخدم من شاشة التعديل كان بيتجاهَل تماماً.**
   الفورم فيه خانة "كلمة المرور الجديدة" في وضع التعديل، لكن الكود
   كان يقرأها ولا يستخدمها أبداً.

4. **الصلاحيات كانت "شكلية" فقط.**
   كل الفحص (`hasPermission`, `checkPageAccess`) كان يحصل في
   الجافاسكريبت جوه المتصفح، وهو فحص يمكن لأي مستخدم تجاوزه بسهولة
   (مثلاً عبر Console أو باستدعاء REST API مباشرة بنفس التوكن بتاعه).
   الحماية الحقيقية الوحيدة هي **Row Level Security (RLS)** جوه
   قاعدة البيانات نفسها.

## الحل اللي جهزته لك

```
supabase/functions/manage-users/index.ts   → Edge Function حقيقية بصلاحية service_role
supabase/sql/01_rls_permissions.sql        → تفعيل RLS حقيقي لكل الجداول
users.html                                  → تعديل الأدمن (Frontend) ليستخدم الحل الجديد
```

- **manage-users**: دالة تعمل على السيرفر (مش المتصفح) وتتحقق أولاً
  إن اللي بيستدعيها فعلاً Admin/Super Admin تابع لنفس الشركة، بعدين
  تنفذ: إنشاء مستخدم حقيقي بدون طرد الأدمن، حذف نهائي حقيقي (auth +
  profile + permissions)، إعادة تعيين كلمة مرور.
- **01_rls_permissions.sql**: يفعّل RLS على كل الجداول المكتشفة في
  المشروع (عملاء، موردين، فواتير، خزينة... إلخ) بحيث قاعدة البيانات
  نفسها ترفض أي عملية غير مسموح بها حسب صلاحيات المستخدم، بغض النظر
  عن الكود في المتصفح.

## خطوات التفعيل (بالترتيب)

### 1) تثبيت Supabase CLI (لو مش مثبت)
```bash
npm install -g supabase
```

### 2) تسجيل الدخول وربط المشروع
```bash
supabase login
supabase link --project-ref ykkhkgajzyxsgoamtmnn
```
(الـ project-ref موجود بالفعل في `supabase-client.js` ضمن رابط SUPABASE_URL)

### 3) نشر الـ Edge Function
```bash
supabase functions deploy manage-users
```
> ملاحظة: متغيرات `SUPABASE_URL` و `SUPABASE_SERVICE_ROLE_KEY` يوفرها
> Supabase تلقائياً لكل Edge Function — مش محتاج تحطهم يدوياً، ومفتاح
> service role **لن يظهر أبداً في كود المتصفح**، وده أهم نقطة أمان هنا.

### 4) تشغيل ملف الـ RLS
- افتح مشروعك في **Supabase Studio → SQL Editor**
- انسخ محتوى `supabase/sql/01_rls_permissions.sql` وشغّله
- **قبل كده**: راجع أسماء الجداول والأعمدة في **Table Editor** وقارنها
  بالموجود في الملف (علّقت على كل افتراض في الملف). لو جدول اسمه أو
  أعمدته مختلفة، عدّل السطر الخاص بيه.
- يُفضّل تجربة الملف أولاً على نسخة تجريبية من قاعدة البيانات لأنه
  يشمل جداول مالية (فواتير وخزينة).

### 5) رفع users.html المعدّل
استبدل ملف `users.html` الحالي في مشروعك بالنسخة المرفقة هنا (نفس
الشكل والتصميم، فقط دوال الإضافة/الحذف/كلمة المرور بقت حقيقية).

### 6) اختبار
- سجّل دخول كأدمن، أضف مستخدم جديد جديد → تأكد إنك **لسه مسجل دخول
  بحساب الأدمن** وإن المستخدم الجديد ظهر في القائمة.
- سجّل دخول بحساب الموظف الجديد بنفس الباسورد اللي حطيته، وجرّب افتح
  صفحة مش مسموح له بيها → المفروض يترفض.
- جرّب "حذف نهائي" لمستخدم تجريبي، وتأكد إنه فعلاً مايقدرش يسجل دخول
  تاني بنفس البيانات.

## لو حابب تمديد الحماية لباقي الصفحات
كل صفحة تانية (invoice.html, treasury.html... إلخ) أصلاً بتعتمد على
نفس جداول قاعدة البيانات، فبمجرد تفعيل RLS في الخطوة 4، الحماية
بتطبّق تلقائياً على كل الصفحات من غير ما تلمس كودها — لأن الفحص بقى
في قاعدة البيانات نفسها مش في كل صفحة لوحدها.
